/**
 * @parent components.seo-export-urls
 * @tag documentation
 * @module {constructor} seo-export-urls/ViewModel seo-export-urls
 */
var $ = require('jquery');
var can = require('can');
require('can/map/define/define');

var ViewModel = can.Map.extend({
  define: {

  }
});

module.exports = ViewModel;
