/**
 * @module {can.Map} assets-operation-badges
 * @parent
 *
 * @description View model of the [status-badges] component.
 *
 * @signature
 * var ViewModel = can.Map.extend({});
 *
 */

var can = require('can');
require('can/map/define/');

var ViewModel = can.Map.extend({
    define: {
    }
});

module.exports = ViewModel;
